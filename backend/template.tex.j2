%-------------------------
% Resume in Latex
% Generated by cv_maker
%------------------------

\documentclass[letterpaper,11pt]{article}

\usepackage{latexsym}
\usepackage[empty]{fullpage}
\usepackage{titlesec}
\usepackage{marvosym}
\usepackage[usenames,dvipsnames]{color}
\usepackage{verbatim}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage[english]{babel}
\usepackage{tabularx}
\input{glyphtounicode}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot{}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

\addtolength{\oddsidemargin}{-0.5in}
\addtolength{\evensidemargin}{-0.5in}
\addtolength{\textwidth}{0.5in}
\addtolength{\topmargin}{-.5in}
\addtolength{\textheight}{1.0in}

\urlstyle{same}
\raggedbottom
\raggedright
\setlength{\tabcolsep}{0in}

\titleformat{\section}{
  \vspace{-4pt}\scshape\raggedright\large
}{}{0em}{}[\color{black}\titlerule \vspace{-5pt}]

\pdfgentounicode=1

%--- Custom commands ---
\newcommand{\resumeItem}[1]{
  \item\small{{#1 \vspace{-2pt}}}
}

\newcommand{\resumeSubheading}[4]{
  \vspace{-2pt}\item
    \begin{tabular*}{0.97\textwidth}[t]{l@{\extracolsep{\fill}}r}
      \textbf{#1} & #2 \\
      \textit{\small#3} & \textit{\small #4} \\
    \end{tabular*}\vspace{-7pt}
}

\newcommand{\resumeProjectHeading}[2]{
    \item
    \begin{tabular*}{0.97\textwidth}{l@{\extracolsep{\fill}}r}
      \small#1 & #2 \\
    \end{tabular*}\vspace{-7pt}
}

\newcommand{\resumeSubItem}[1]{\resumeItem{#1}\vspace{-4pt}}

\renewcommand\labelitemii{$\vcenter{\hbox{\tiny$\bullet$}}$}

\newcommand{\resumeSubHeadingListStart}{\begin{itemize}[leftmargin=0.15in, label={}]}
\newcommand{\resumeSubHeadingListEnd}{\end{itemize}}
\newcommand{\resumeItemListStart}{\begin{itemize}}
\newcommand{\resumeItemListEnd}{\end{itemize}\vspace{-5pt}}

%-------------------------------------------

\begin{document}

%---------- HEADER ----------
\begin{center}
    \textbf{\Huge \scshape <{ tex(cv.header.name) }>} \\[5pt]
    <{ tex(cv.header.email) }>%
    << if cv.header.linkedin >> $|$
    \href{<{ cv.header.linkedin }>}{\underline{<{ tex(cv.header.linkedin) }>}}%
    << endif >>
    << for field in cv.header.extra >>
    << if field.value >> $|$
    << if field.value.startswith('http') >>
    \href{<{ field.value }>}{\underline{<{ tex(field.value) }>}}%
    << else >>
    <{ tex(field.value) }>%
    << endif >>
    << endif >>
    << endfor >> \\[5pt]
    <{ tex(cv.header.location) }>
\end{center}

%---------- SUMMARY ----------
<< if cv.summary >>
\section{Summary}
  \resumeItemListStart
  << for item in cv.summary >>
    \resumeItem{<{ tex(item) }>}
  << endfor >>
  \resumeItemListEnd
<< endif >>

%---------- EDUCATION ----------
<< if cv.education >>
\section{Education}
  \resumeSubHeadingListStart
  << for edu in cv.education >>
    \resumeSubheading
      {<{ tex(edu.institution) }>}{<{ tex(edu.location) }>}
      {<{ tex(edu.degree) }>}{<{ tex(edu.dates) }>}
    << if edu.note >>
      \resumeItemListStart
        \resumeItem{\textbf{<{ tex(edu.note) }>}}
      \resumeItemListEnd
    << endif >>
  << endfor >>
  \resumeSubHeadingListEnd
<< endif >>

%---------- PROFESSIONAL EXPERIENCE ----------
<< if cv.experience >>
\section{Professional Experience}
\resumeSubHeadingListStart
  << for exp in cv.experience >>
  \resumeSubheading
    {<{ tex(exp.title) }>}{<{ tex(exp.dates) }>}
    {<{ tex(exp.company) }>}{<{ tex(exp.location) }>}
    << if exp.bullets >>
    \resumeItemListStart
      << for bullet in exp.bullets >>
      \resumeItem{<{ tex(bullet) }>}
      << endfor >>
    \resumeItemListEnd
    << endif >>
  << endfor >>
\resumeSubHeadingListEnd
<< endif >>

%---------- PROJECT EXPERIENCE ----------
<< if cv.projects >>
\section{Project Experience}
    \resumeSubHeadingListStart
    << for proj in cv.projects >>
      \resumeProjectHeading
          {\textbf{<{ tex(proj.name) }>} << if proj.role >> $|$ \emph{<{ tex(proj.role) }>}<< endif >>}{<{ tex(proj.date) }>}
          << if proj.bullets >>
          \resumeItemListStart
            << for bullet in proj.bullets >>
            \resumeItem{<{ tex(bullet) }>}
            << endfor >>
          \resumeItemListEnd
          << endif >>
    << endfor >>
    \resumeSubHeadingListEnd
<< endif >>

%---------- TECHNICAL SKILLS ----------
<< if cv.skills >>
\section{Technical Skills}
 \begin{itemize}[leftmargin=0.15in, label={}]
    \small{\item{
    << for category, value in cv.skills.items() >>
     \textbf{<{ tex(category) }>}{: <{ tex(value) }>} \\
    << endfor >>
    }}
 \end{itemize}
<< endif >>

%-------------------------------------------
\end{document}
